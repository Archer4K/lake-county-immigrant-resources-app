"use client";
import { useState } from "react";
export default function Login(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const values=Object.fromEntries(new FormData(e.currentTarget).entries());const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)}).catch(()=>null);setBusy(false);if(response?.ok)location.reload();else setError("Password was not recognized.")}return <main className="admin-shell"><a href="/">← Directory</a><h1>Resource review</h1><p>Sign in to approve organization requests.</p><form onSubmit={submit} style={{maxWidth:380,display:"grid",gap:12}}><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="primary" disabled={busy}>Sign in</button>{error&&<p role="alert">{error}</p>}</form></main>}
