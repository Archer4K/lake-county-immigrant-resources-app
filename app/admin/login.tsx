"use client";
import { SiteNav, useLanguage } from "../language";
import { adminCopy } from "./copy";
import { useState } from "react";
export default function Login(){const {language}=useLanguage();const t=adminCopy[language];const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const values=Object.fromEntries(new FormData(e.currentTarget).entries());const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)}).catch(()=>null);setBusy(false);if(response?.ok)location.reload();else setError("error")}return <main className="admin-shell"><SiteNav current="directory"/><h1>{t.title}</h1><p>{t.intro}</p><form onSubmit={submit} style={{maxWidth:380,display:"grid",gap:12}}><label>{t.password}<input name="password" type="password" required autoComplete="current-password"/></label><button className="primary" disabled={busy}>{t.sign}</button>{error&&<p role="alert">{t.error}</p>}</form></main>}
